{
  "name": "stocksense-mcp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx -y mongodb-mcp-server@latest --transport http --httpHost 0.0.0.0 --httpPort 8080 --disabledTools atlas-local-create-deployment,atlas-local-delete-deployment,create-collection,create-index,delete-many,drop-collection,drop-database,drop-index,rename-collection"
  },
  "engines": {
    "node": ">=20"
  }
}
